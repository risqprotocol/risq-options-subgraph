{
  "name": "roption",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ everyhealth/roption",
    "create-local": "graph create --node http://localhost:8020/ everyhealth/roption",
    "remove-local": "graph remove --node http://localhost:8020/ everyhealth/roption",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 everyhealth/roption"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
