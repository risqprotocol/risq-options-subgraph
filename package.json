{
  "name": "roption",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ risqprotocol/risq-options-subgraph",
    "create-local": "graph create --node http://localhost:8020/ risqprotocol/risq-options-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ risqprotocol/risq-options-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 risqprotocol/risq-options-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.0",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
